import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'space-evenly',
        alignItems: 'center',
        borderWidth: 0.5,
        borderColor: 'gray',
        padding: 10
    },
    texto: {
        textAlign: 'center',
        color: 'black',
        fontSize: 13,
        fontWeight: 'bold'
    }
});