import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        borderWidth: 0.5,
        borderColor: 'ghostwhite',
        padding: 25
    },
    image: {
        width: '100%',
        height: '100%',
        resizeMode: 'contain'
    }
});